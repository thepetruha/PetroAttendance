const express = require('express');
const fs = require('fs');

const app = express();
var router = express.Router();

router.route('/petro/api/export')
.post((req, res) => {
    
})

module.exports = router;
